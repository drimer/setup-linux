class common_user_files {
  file { "@HOME_DIR@/.bashrc":
    ensure => present,
  }

  file { "@HOME_DIR@/.bash_profile":
    ensure => present,
  }

  file { "@HOME_DIR@/src":
    ensure => "directory",
  }

  file { "@HOME_DIR@/bin":
    ensure => "directory",
  }
}
